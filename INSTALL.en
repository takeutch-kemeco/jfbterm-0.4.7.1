jfbterm is autoconfisicated since version 0.2.0.

To install jfbterm, run

	./autogen.sh
	./configure --prefix=/usr --sysconfdir=/etc
	make
	sudo make install

You need to create /usr/local/etc/jfbterm.conf from jfbterm.conf.sample.
Also you need font files that you configured in jfbterm.conf

